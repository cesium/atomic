<! --
= form_for @activity, html: {multipart: true} do |form|

  - if @activity.errors.any?
    div.alert class="alert-danger"
      - error_messages(@activity).each do |msg|
        li
          = msg

  = form.label :name,
    class: "activity-form-label"
  br
  = form.text_field :name,
    class: "activity-form-field"

  = form.label :location,
    class: "activity-form-label"
  br
  = form.text_field :location,
    class: "activity-form-field"

  = form.label :description,
    class: "activity-form-label"
  br
  = form.text_area :description,
    class: "activity-form-field"

  = form.label :speaker,
    class: "activity-form-label"
  br
  = form.text_field :speaker,
    class: "activity-form-field"

  = form.label :member_cost,
    class: "activity-form-label"
  br
  = form.number_field :member_cost,
    value: 0,
    class: "activity-form-field"

  = form.label :guest_cost,
    class: "activity-form-label"
  br
  = form.number_field :guest_cost,
    value: 0,
    class: "activity-form-field"

  = form.label :start_date,
    class: "activity-form-label"
  br
  = form.datetime_select :start_date
  br

  = form.label :end_date,
    class: "activity-form-label"
  br
  = form.datetime_select :end_date
  br

  = form.label :coffee_break,
    class: "activity-form-label"
  br
  = form.select :coffee_break,
    options_for_select([['No', false], ['Yes', true]])
  br

  = form.label :activity,
    class: "activity-form-label"
  br
  = form.select :activity_id,
    options_for_select(Activity.all.map{|a|[a.name, a.id]}),
    {include_blank: 'None'}
  br

  = form.label :external_link,
    class: "activity-form-label"
  br
  = form.text_field :external_link,
    class: "activity-form-field"
  br

  = form.label :allows_registrations,
    class: "activity-form-label"
  br
  = form.check_box :allows_registrations,
    class: "activity-form-field"
  br
  div#allows_registration hidden=""
    = form.label :limit_number_participants,
      class: "activity-form-label"
    br
    = form.check_box :limit_number_participants,
      class: "activity-form-field"
    br
    div#is-number-participants-limited hidden=""
      = form.label :number_participants,
        class: "activity-form-label"
      br
      = form.number_field :number_participants,
        value: 20,
        class: "activity-form-field"
  br
  = form.label :poster,
    class: "activity-form-label"
  br
  = form.file_field :poster,
    class: "activity-form-field"
  br

  .submit-field
    = form.submit class: "btn btn-lg btn-update border-theme"
    - if @activity.id != nil
      = link_to 'Cancel', activity_path(@activity),
        data: { confirm: 'You will lose all modifications. Are you sure?' },
        class: "btn btn-lg border-theme"
      = link_to 'Destroy', :activity, method: :delete,
        data: { confirm: 'Are you sure you want to delete this activity?' },
        class: "btn btn-lg btn-delete border-theme"

-- >


<% _slim_controls1 = form_for @activity, html: {multipart: true} do |form| %><% _slim_controls2 = '';
;
; if @activity.errors.any?
; _slim_controls2 << ("<div".freeze); _slim_controls2 << (" class=\"".freeze); _slim_controls2 << ("alert".freeze); _slim_controls2 << (" ".freeze); _slim_controls2 << ("alert-danger".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (">".freeze);
; error_messages(@activity).each do |msg|
; _slim_controls2 << ("<li".freeze); _slim_controls2 << (">".freeze);
; _slim_controls2 << ((msg).to_s);
;
; _slim_controls2 << ("</li>".freeze); end; _slim_controls2 << ("</div>".freeze); end; _slim_controls2 << ((form.label :name,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.text_field :name,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :location,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.text_field :location,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :description,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.text_area :description,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :speaker,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.text_field :speaker,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :member_cost,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.number_field :member_cost,
value: 0,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :guest_cost,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.number_field :guest_cost,
value: 0,
class: "activity-form-field").to_s);
;
; _slim_controls2 << ((form.label :start_date,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.datetime_select :start_date).to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ((form.label :end_date,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.datetime_select :end_date).to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ((form.label :coffee_break,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.select :coffee_break,
options_for_select([['No', false], ['Yes', true]].to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ((form.label :activity,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.select :activity_id,
options_for_select(Activity.all.map{|a|[a.name, a.id]}),
{include_blank: 'None'}).to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ((form.label :external_link,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.text_field :external_link,
class: "activity-form-field").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ((form.label :allows_registrations,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.check_box :allows_registrations,
class: "activity-form-field").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ("<div".freeze); _slim_controls2 << (" hidden=\"".freeze); _slim_controls2 << ("".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (" id=\"".freeze); _slim_controls2 << ("allows_registration".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (">".freeze);
; _slim_controls2 << ((form.label :limit_number_participants,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.check_box :limit_number_participants,
class: "activity-form-field").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ("<div".freeze); _slim_controls2 << (" hidden=\"".freeze); _slim_controls2 << ("".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (" id=\"".freeze); _slim_controls2 << ("is-number-participants-limited".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (">".freeze);
; _slim_controls2 << ((form.label :number_participants,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.number_field :number_participants,
value: 20,
class: "activity-form-field").to_s);
; _slim_controls2 << ("</div>".freeze); _slim_controls2 << ("</div>".freeze); _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.label :poster,
class: "activity-form-label").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
; _slim_controls2 << ((form.file_field :poster,
class: "activity-form-field").to_s);
; _slim_controls2 << ("<br".freeze); _slim_controls2 << (" />".freeze);
;
; _slim_controls2 << ("<div".freeze); _slim_controls2 << (" class=\"".freeze); _slim_controls2 << ("submit-field".freeze); _slim_controls2 << ("\"".freeze); _slim_controls2 << (">".freeze);
; _slim_controls2 << ((form.submit class: "btn btn-lg btn-update border-theme").to_s);
; if @activity.id != nil
; _slim_controls2 << ((link_to 'Cancel', activity_path(@activity),
data: { confirm: 'You will lose all modifications. Are you sure?' },
class: "btn btn-lg border-theme").to_s);
; _slim_controls2 << ((link_to 'Destroy', :activity, method: :delete,
data: { confirm: 'Are you sure you want to delete this activity?' },
class: "btn btn-lg btn-delete border-theme").to_s);
; end; _slim_controls2 << ("</div>".freeze); _slim_controls2 %><% end %><%= _slim_controls1 %>
